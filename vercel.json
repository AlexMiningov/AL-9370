{
  "buildCommand": "cd packages/web && bun run build",
  "outputDirectory": "packages/web/dist",
  "installCommand": "cd packages/web && bun install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
